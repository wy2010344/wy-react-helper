{
  "name": "@wy-react-helper/workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "wy-react-helper workspace root with Git hooks and commit standards",
  "scripts": {
    "commit": "cz",
    "prepare": "husky",
    "lint": "cd base && pnpm run lint && cd ../dom && pnpm run lint && cd ../native && pnpm run lint",
    "lint:check": "cd base && pnpm run lint:check && cd ../dom && pnpm run lint:check && cd ../native && pnpm run lint:check",
    "format": "cd base && pnpm run format && cd ../dom && pnpm run format && cd ../native && pnpm run format",
    "format:check": "cd base && pnpm run format:check && cd ../dom && pnpm run format:check && cd ../native && pnpm run format:check",
    "type-check": "cd base && pnpm run type-check && cd ../dom && pnpm run type-check && cd ../native && pnpm run type-check",
    "build": "cd base && pnpm run build && cd ../dom && pnpm run build && cd ../native && pnpm run build"
  },
  "devDependencies": {
    "commitizen": "catalog:",
    "cz-conventional-changelog": "catalog:",
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "husky": "catalog:",
    "lint-staged": "catalog:",
    "@repo/eslint-config": "workspace:*",
    "@repo/prettier-config": "workspace:*",
    "eslint": "catalog:",
    "prettier": "catalog:"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
